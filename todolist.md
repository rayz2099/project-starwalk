# !!!SCRUM!!!

# DESIGN REQUEST
生成个 todolist 把 我交给另一个 agent 实现
init 到 一直迭代 验收完成 的全流程 todolist

围绕当前仓库的 `Stargazing Planner` 单页项目，基于既定约束推进：
- 使用 `Bun + React.js App Router + TypeScript + Tailwind CSS + shadcn/ui`
- 页面目标是“多地点 x 多日期”的观星条件对比矩阵
- 月相与日月出落严格使用 `suncalc`
- 天气数据统一使用 `Open-Meteo`
- 服务端完成天气请求、夜间切片、聚合与评分
- UI 为单页，包含顶部控制区和下方矩阵
- 需要从初始化、实现、联调、迭代修正一路推进到验收完成

# UPDATES
- 2026-04-24: 新建全流程交接 todo，目标是让下一个 agent 可以按顺序独立完成实现与验收闭环。

# TASKS
- [ ] TASK No.1: 初始化项目脚手架与基础工程配置
- [ ] TASK No.2: 安装并配置 UI 基础设施
- [ ] TASK No.3: 建立项目目录骨架与共享类型
- [ ] TASK No.4: 落地静态地点配置与业务常量
- [ ] TASK No.5: 实现日期范围与矩阵输入模型
- [ ] TASK No.6: 实现 Open-Meteo 服务端天气客户端
- [ ] TASK No.7: 实现 `suncalc` 月相与夜间辅助计算模块
- [ ] TASK No.8: 实现夜间窗口切片与气象聚合
- [ ] TASK No.9: 实现观星评分与风险判定纯函数
- [ ] TASK No.10: 实现服务端编排入口与矩阵 DTO
- [ ] TASK No.11: 实现顶部控制区 UI
- [ ] TASK No.12: 实现主对比矩阵 UI
- [ ] TASK No.13: 实现页面装配与初始交互闭环
- [ ] TASK No.14: 处理错误态、空态与加载态
- [ ] TASK No.15: 完成首轮人工联调与数据校验
- [ ] TASK No.16: 补充关键自动化验证
- [ ] TASK No.17: 根据联调问题做第一轮迭代修正
- [ ] TASK No.18: 根据产品体验做第二轮可用性迭代
- [ ] TASK No.19: 完成验收清单、自检记录与交付说明

## TASK No.1: 初始化项目脚手架与基础工程配置
### what to be done
使用 `Bun` 初始化 `React.js` 项目，启用 `App Router`、`TypeScript`、`Tailwind CSS`，确保开发、构建、格式化脚本可用；补齐基础配置文件，使仓库达到“可启动、可编译”的最小状态。
### rationale
这是全部任务的编译前置条件。先把运行时、路由模式和构建链稳定下来，后续所有功能开发才能围绕同一工程基线展开。

## TASK No.2: 安装并配置 UI 基础设施
### what to be done
接入 `shadcn/ui`，安装项目所需组件与依赖，至少覆盖 `table`、`card`、`badge`、`calendar` 或日期选择相关能力；同时确认 Tailwind 主题、暗色方案和全局样式可以支撑天文主题页面。
### rationale
页面是矩阵型信息密集界面，基础组件必须尽早稳定，否则后面展示层和交互层会反复返工。

## TASK No.3: 建立项目目录骨架与共享类型
### what to be done
按 `AGENTS.md` 约束创建推荐目录结构，拆出 `config`、`features/stargazing/components`、`features/stargazing/server`、`types`、`constants` 等模块；定义地点、日期范围、小时天气、夜间聚合、评分结果、矩阵 DTO 等共享类型。
### rationale
先明确边界和数据契约，等价于后端里的接口协议设计。这样后续服务端编排和前端渲染可以并行而不互相污染。

## TASK No.4: 落地静态地点配置与业务常量
### what to be done
建立静态地点配置文件，录入首批观测点的 `id/name/latitude/longitude/elevation`；统一沉淀观测窗口、评分阈值、默认日期范围、默认选中地点等业务常量。
### rationale
地点与阈值是全局输入源。先集中管理，避免在实现过程中到处散落 hardcode，降低后续调整成本。

## TASK No.5: 实现日期范围与矩阵输入模型
### what to be done
定义页面输入模型，包括日期范围、地点多选、默认值、校验规则、服务端入参序列化方式；明确矩阵的列生成规则和每个“业务日”对应的夜间窗口语义。
### rationale
你的核心域模型不是天气点查，而是“多地点、多日期”的批量对比。输入模型必须先定清，否则后端聚合和前端渲染会出现语义错位。

## TASK No.6: 实现 Open-Meteo 服务端天气客户端
### what to be done
实现服务端天气请求模块，按官方字段请求 hourly 数据：`temperature_2m`、`dew_point_2m`、`cloud_cover`、`cloud_cover_low`、`cloud_cover_high`；支持多地点批量请求或服务端并发请求，处理超时、非 200、字段缺失和可预见的上游限流。
### rationale
真实天气是整个页面的数据基础。这一层要先保证输入输出稳定，后面的聚合与评分才能建立在可靠源头上。

## TASK No.7: 实现 `suncalc` 月相与夜间辅助计算模块
### what to be done
基于 `suncalc` 封装月相亮度、月相展示文案、月相图标映射，以及页面展示所需的基础天文信息；保证计算在服务端完成，避免 UI 层自行推导。
### rationale
用户需求明确要求月相严格离线本地算，这部分相当于核心业务规则的一半，必须单独成层并保持纯净。

## TASK No.8: 实现夜间窗口切片与气象聚合
### what to be done
基于每个目标日期，将 hourly 数据切成该业务日 `20:00` 到次日 `04:00` 的夜间窗口；计算总云量平均值/最大值、低云平均值、高云平均值、最低温度、最小温露点差，并输出结构化聚合结果。
### rationale
这是本项目最核心的领域逻辑，等同于把原始时序数据转成可决策的数据摘要。矩阵格子展示的就是这里的结果。

## TASK No.9: 实现观星评分与风险判定纯函数
### what to be done
按照既定规则实现 `POOR / FAIR / EXCELLENT` 评分纯函数，并同时产出 UI 所需的风险字段，例如结露/起雾风险、云量摘要、月相影响说明。
### rationale
评分规则必须独立、纯净、可验证，避免被 UI 展示代码耦合。这样后续调阈值或补测试时成本最低。

## TASK No.10: 实现服务端编排入口与矩阵 DTO
### what to be done
实现单一入口的编排模块，将地点配置、日期范围、天气请求、月相计算、夜间聚合和评分串起来，最终返回页面直接消费的矩阵 DTO；可使用 Server Action 或服务端函数，但职责必须清晰。
### rationale
这一步相当于应用服务层。前端页面不应该知道底层抓了哪些数据源、怎么切片、怎么评分，只应该拿到干净结果。

## TASK No.11: 实现顶部控制区 UI
### what to be done
实现顶部控制区，包括日期范围选择、地点多选、默认值回填、提交或自动刷新策略；让输入状态能够稳定驱动服务端重算。
### rationale
控制区是用户触发业务流程的入口。只有这里稳定，后续矩阵的数据刷新闭环才成立。

## TASK No.12: 实现主对比矩阵 UI
### what to be done
实现矩阵表格：列为日期，行为地点；列头展示日期与月相信息，单元格使用卡片呈现评分、云量、月相百分比和结露风险提示；保证桌面端优先、移动端可用。
### rationale
这是用户真正消费结果的主视图，也是产品价值的直接承载面，必须在结构和可读性上优先保证。

## TASK No.13: 实现页面装配与初始交互闭环
### what to be done
在 `app/page.tsx` 中完成页面装配，接通控制区与矩阵区的交互闭环，使默认页面打开即可看到首屏结果，并能切换日期/地点触发数据更新。
### rationale
单个模块都完成不等于产品可用。需要尽早形成从输入到结果的最小闭环，才能开始真实联调。

## TASK No.14: 处理错误态、空态与加载态
### what to be done
补齐加载中、请求失败、无数据、部分地点失败等状态的 UI 与提示文案；对于不可计算状态必须显式暴露，不允许伪装成正常观测条件。
### rationale
天气源和日期窗口天然有不确定性。如果这层不补齐，页面在真实环境下的可靠性会很差，也会误导用户做观测决策。

## TASK No.15: 完成首轮人工联调与数据校验
### what to be done
围绕若干固定地点和固定日期做人工核对，检查矩阵维度、列日期、月相显示、夜间切片、评分结果、风险提示是否一致；必要时对照 Open-Meteo 原始 hourly 数据做 spot check。
### rationale
这一步是领域正确性的第一次验算，类似后端把聚合结果和原始库表做交叉核对，能尽早发现算法偏差和时区/跨天错误。

## TASK No.16: 补充关键自动化验证
### what to be done
为纯函数和核心服务端逻辑补充自动化验证，至少覆盖跨天夜间切片、月相阈值判定、温露点差小于 `2°C` 的风险判定、多地点多日期矩阵映射正确性等关键场景。
### rationale
首轮人工联调只能证明“当前样例大致正确”，自动化验证才能防止后续迭代把核心逻辑带坏。

## TASK No.17: 根据联调问题做第一轮迭代修正
### what to be done
修复首轮联调和自动化验证中暴露的问题，重点优先处理：时区问题、跨天切片问题、字段缺失兼容、评分条件覆盖顺序、矩阵渲染错位。
### rationale
这是从“能跑”走向“可信”的第一轮收敛，必须优先消除会直接影响决策结果的逻辑错误。

## TASK No.18: 根据产品体验做第二轮可用性迭代
### what to be done
围绕可读性和交互体验继续微调，包括卡片信息密度、颜色语义、提示文案、默认筛选组合、移动端可用性、表格滚动体验等；但不得突破单页范围和当前 MVP 边界。
### rationale
首轮修正后页面通常已经逻辑正确，但使用体验未必顺手。这一轮的目标是把“正确”变成“好用”，同时控制范围不失焦。

## TASK No.19: 完成验收清单、自检记录与交付说明
### what to be done
输出最终验收清单与交付说明，确认开发、构建、页面展示、主要交互、关键评分逻辑、错误态处理都已通过；记录已知限制、后续可选增强项和运行方式，形成可交接结果。
### rationale
没有验收闭环，任务就只是“写过代码”。这一项把实现、验证、已知风险和交付方式沉淀下来，方便继续迭代或正式接手。

# Impact to the Knowledge Base
- 当前产物是项目级任务分解文档，可直接作为后续 design / plan / execute 阶段输入。
- 任务顺序已按依赖、编译优先、核心优先、验证后置组织，适合交给另一个 agent 顺序推进。
- 该清单已显式覆盖“初始化、实现、联调、两轮迭代、验收交付”全流程，后续无需再重复拆总任务。

# !!!FINISHED!!!
