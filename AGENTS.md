# AGENTS.md

本文件覆盖当前仓库默认协作约束，面向 `Stargazing Planner` 单页应用。

## 1. 项目目标

- 目标是构建一个单页 Web 应用，用于横向对比多个观测点、多个日期的观星条件。
- 当前阶段只做一个页面，不引入登录、数据库、地图编辑、后台管理。
- 决策核心不是“天气详情页”，而是“多地点 x 多日期”的观测条件矩阵。

## 2. 强制技术栈

- 运行时与包管理：`Bun`
- 框架：`React.js`，必须使用 `App Router`
- 语言：`TypeScript`
- UI：`Tailwind CSS` + `shadcn/ui`
- 日期处理：`date-fns`
- 月相与日月出落：`suncalc`
- 天气数据：`Open-Meteo Forecast API`

## 3. 数据源硬约束

### 3.1 地点数据

- 地点配置必须静态保存在代码中，不接数据库，不接地理编码服务。
- 每个地点至少包含：
  - `id`
  - `name`
  - `latitude`
  - `longitude`
  - `elevation`
- 推荐放在 `src/config/locations.ts`。

### 3.2 月相数据

- 月相、月亮亮度、日月出落一律使用 `suncalc` 本地计算。
- 禁止为了月相再接任何第三方 API。
- 月相计算必须放在服务端模块中，避免 UI 层重复计算和时区偏差。

### 3.3 天气数据

- 天气 API 统一使用 `Open-Meteo Forecast API`。
- 当前实现必须请求逐小时字段：
  - `temperature_2m`
  - `dew_point_2m`
  - `cloud_cover`
  - `cloud_cover_low`
  - `cloud_cover_high`
- 注意：使用 Open-Meteo 当前字段名，不要使用旧字段名 `cloudcover`、`cloudcover_low`、`cloudcover_high`、`dewpoint_2m`。
- 所有外部天气请求必须在服务端完成，禁止浏览器直接请求第三方 API。

## 4. 架构约束

### 4.1 页面结构

- 仅保留一个入口页面：`app/page.tsx`
- `page.tsx` 只负责组装页面，不承载复杂业务计算。
- 控制台区域与结果矩阵区域必须拆成独立组件。

### 4.2 推荐目录

```text
app/
  page.tsx
  actions.ts
src/
  config/
    locations.ts
  features/
    stargazing/
      components/
        control-panel.tsx
        planner-matrix.tsx
        planner-cell-card.tsx
      server/
        open-meteo.ts
        moon.ts
        planner.ts
      types.ts
      constants.ts
```

### 4.3 分层规则

- `components/`：只负责展示和交互，不直接调用第三方 API。
- `server/open-meteo.ts`：只负责天气请求与响应映射。
- `server/moon.ts`：只负责月相、月亮亮度、出落时间计算。
- `server/planner.ts`：负责夜间切片、聚合、评分，是核心编排层。
- `types.ts`：定义输入、聚合结果、评分枚举，避免页面里散落匿名类型。

## 5. 核心业务规则

### 5.1 夜间窗口

- 观测窗口默认定义为本地时间 `20:00` 到次日 `04:00`。
- 夜间切片必须基于地点本地日期语义，不要直接按 UTC 切。
- 单日卡片展示的是“该日夜间窗口”的条件，允许跨天取小时数据。

### 5.2 聚合指标

- 夜间窗口内至少聚合以下指标：
  - 总云量平均值
  - 总云量最大值
  - 低云量平均值
  - 高云量平均值
  - 最低温度
  - 最小温露点差
- `最小温露点差 = min(temperature_2m - dew_point_2m)`
- 所有聚合逻辑必须集中在同一个服务端模块，避免组件层二次推导。

### 5.3 评分算法

- 定义明确枚举：`POOR`、`FAIR`、`EXCELLENT`
- 评分顺序必须固定，避免条件覆盖歧义：
  1. 若月相亮度 `> 70%`，判定为 `POOR`
  2. 若夜间总云量平均值或最大值 `> 60%`，判定为 `POOR`
  3. 若总云量 `< 15%` 且月相亮度 `< 30%`，判定为 `EXCELLENT`
  4. 若最小温露点差 `< 2°C`，判定为 `FAIR`
  5. 其余判定为 `FAIR`
- 评分函数必须是纯函数，输入聚合结果，输出结构化评分对象。

## 6. UI 约束

### 6.1 布局

- 页面固定为两段式：
  - 顶部控制区：日期范围选择 + 地点多选
  - 下方主矩阵：日期为列，地点为行
- 页面重点是可比较性，不做信息流式卡片瀑布。

### 6.2 组件要求

- 必须使用 `shadcn/ui` 中适合的基础组件：
  - `table`
  - `card`
  - `badge`
  - `calendar` 或 date-picker 方案
- 单元格卡片至少展示：
  - 评级 Badge
  - 云量核心值
  - 月相百分比
  - 起雾/结露风险提示

### 6.3 视觉风格

- 采用深色背景或极简科技风，但优先可读性，不要把“天文主题”做成装饰噪音。
- 颜色语义必须稳定：
  - `EXCELLENT` = 绿色
  - `FAIR` = 黄色
  - `POOR` = 红色
- 表头日期应附带月相图标或文本提示，例如 `4/25 🌔 75%`。

## 7. API 调用策略

- 优先使用 Open-Meteo 的多坐标批量查询能力，减少 N 次独立请求的往返成本。
- 如果批量模式影响响应映射可读性，再退回服务端并发请求，但禁止在客户端并发拉取。
- 必须显式处理以下问题：
  - 上游接口超时
  - 某个地点数据缺失
  - 日期范围超出 API 支持窗口
  - 小时数组长度不一致
- 错误处理要暴露“不可计算”状态，不允许偷偷 fallback 成正常天气。

## 8. 代码规范

- 所有新增或修改的类、函数必须写必要的中文注释，重点解释 `why`。
- 禁止写 `fallback` 风格逻辑，除非计划文档明确批准。
- 单文件不得超过 `1000` 行，接近 `500` 行就要主动拆分。
- 保持纯函数和数据转换集中，避免在 React 组件里写大段业务逻辑。
- 禁止把时间、评分阈值、默认地点写死在多个文件里，统一放到 `constants.ts`。

## 9. 测试与验证

- 至少覆盖以下验证：
  - 日期范围跨天切片正确
  - 月相亮度阈值判定正确
  - 温露点差 `< 2°C` 的起雾风险正确
  - 多地点矩阵映射不串列、不串行
- 如果暂时不写自动化测试，至少保留可单独调用的纯函数供后续补测。

## 10. 当前阶段不做的事

- 不做用户系统
- 不做持久化存储
- 不做地图选点
- 不做分享、导出图片、订阅提醒
- 不做 astronomy seeing / transparency 的高级天文气象模型

## 11. 推荐实现顺序

1. 初始化 `React.js + Tailwind + shadcn/ui`
2. 建静态地点配置
3. 封装 Open-Meteo 服务端请求
4. 封装 `suncalc` 月相计算
5. 完成夜间切片 + 聚合 + 评分
6. 最后渲染矩阵页面与交互控件
